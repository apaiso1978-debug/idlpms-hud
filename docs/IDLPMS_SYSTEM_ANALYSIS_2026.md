# 🔍 IDLPMS System Analysis Report (Update 2026-02-22)

**Project:** Integrated Digital Learning Platform Management System (IDLPMS)
**Status:** In Active Development (Pre-production Integration Phase)
**Core Concept:** "Instructional Operating System" (Educational OS that feels like a professional IDE / VS Code)

---

## 1. ฐานข้อมูลและสถาปัตยกรรมข้อมูล (System Architecture Guard)

สถาปัตยกรรมหลักยึดกฎทองคำ 4 ข้อ (The 4 Core Commandments):

1. **Person-First Schema:** ระบบมองผู้ใช้เป็น "บุคคล (Person)" (อิงตามเลขบัตร ปชช. หรือรหัสประจำตัว) บทบาท (Student, Teacher, Director) เป็นเพียงแค่สถานะชั่วคราว ข้อมูลหลักทั้งหมด (DNA, Health) ผูกติดกับ `persons`
2. **Edge-to-Edge Fluidity (Rule 21):** แดชบอร์ดเป็นแบบ Command Center ห้ามจำกัดความกว้างของหน้า (`max-w-*`) โมดูลต่างๆ ต้องขยายเต็มขอบซ้าย-ขวา (`w-full`) เสมอ ยกเว้น Padding มาตรฐาน 24px (`px-6`)
3. **Premium Smart Notify:** ห้ามใช้ `alert()`, `confirm()` ของ Native Browser เด็ดขาด ต้องใช้ `showSmartNotify(message, type)` ซึ่งออกแบบมาสอดคล้องกับธีม Neon / Dark mode ของระบบ
4. **Offline-First & Sync Engine:** ระบบออกแบบมาเพื่อโรงเรียนที่มีอินเทอร์เน็ตไม่เสถียร ต้องเขียนข้อมูลลง `LocalDataService` ก่อนเสมอเพื่อให้ UI อัปเดตทันที แล้วจึงส่งต่อให้ `SyncEngine.js` ทำงานแบบ Background Persistent Sync

---

## 2. กฎการออกแบบ UI และ CSS (Razor-Sharp Design Guard)

IDLPMS ใช้โครงสร้างแบบ **Vanila HTML/JS + CSS Custom Properties** (ไม่มี React/Vue) เพื่อประสิทธิภาพสูงสุด กฎของ UI เข้มงวดมาก:

### 2.1 โครงสร้างและระยะ (Structure & Alignment)
- **48px Vertical Rule:** Header ของ Main Stage, Sidebar Panel สูง 48px เสมอ
- **4px Grid System:** Padding/Margin ต้องหาร 4 ลงตัว (4, 8, 12, 16, 24, 32, 48) เท่านั้น
- **Rule 20 (Horizontal Alignment Unity):** ระยะขอบหน้า (Padding) ฝั่งซ้าย-ขวาต้องเป็น `24px` (`px-6`) ทุกเพจ เพื่อให้ Breadcrumbs ซ้ายสุด และปุ่ม Action ขวาสุด ตรงเป็นเส้นเดียวกันตลอดเวลาเมื่อสลับหน้า
- **Strict 3px Corners:** รัศมีขอบมุม (Border Radius) บังคับใช้ `3px` (`var(--vs-radius)`) ทุกองค์ประกอบ ห้ามให้กลมกว่านี้ (ไม่อนุญาต `rounded-lg`, `rounded-xl`)
- **Global Line Unity:** เส้นขอบ (Border) บังคับใช้ความหนา 1px บางแบบ **50% Opacity** เท่านั้น (`rgba(63,63,70,0.5)`)

### 2.2 โทนสีและความลึก (Color & Depth - Zinc Hierarchy)
- **ห้ามใช้สีเงาตื้น (Drop Shadow):** ใช้ Glow Effects จากสี Neon เท่านั้น
- **ระบบความลึก (Zinc Elevation):**
  - **Level 1 (Floor):** `--vs-bg-deep` (Zinc-850) พื้นหลังหลักและช่อง Input
  - **Level 2 (Panel):** `--vs-bg-panel` (Zinc-800) แถบข้าง, Header
  - **Level 3 (Surface):** `--vs-bg-card` (Zinc-750) โมดูลตั้งบนหน้าจอ
- **Groove Dividers:** เส้นคั่น (Divider) ต้องมี "แสงเงา 2 ชั้น" (เส้นขอบมืดคู่เส้นสว่างแบบฝัง) โดยใช้ CSS Class `.border-b border-[var(--vs-border)]` ใน Tailwind เพื่อการคำนวณอัตโนมัติ 
- **Sunken Inset (Input Fields):** ช่องกรอกข้อมูล (Input) ต้อง "บุ๋ม" ลงไปที่ Level 1 (ไม่มีขอบเส้น อาศัยความลึกของสีอย่างเดียว)

### 2.3 ปุ่มพรีเมียม (Neon Style Policy)
- **ห้ามระบายสีทึบ (Zero Solid Fill):** สีพื้นหลังปุ่ม/ป้ายกำกับ ต้องโป่งใส (10-15% Opacity) พร้อมข้อความสีสดใส (Accent) และขอบที่มีแสงเรืองรองบางๆ (30% Glow border)
- ตัวอย่างตัวแปรสี Accent:
  - Cyan (`--vs-accent`): แนวทางหลัก (CTA)
  - Green (`--vs-success`): ตกลง / สำเร็จ
  - Red (`--vs-danger`): ปฏิเสธ / ล้มเหลว

### 2.4 ตัวอักษรและไอคอน (Typography & Iconography)
- **ฟอนต์ Sarabun 300(Light):** 100% ตัวอักษรบังคับใช้ขนาด `13px` ด้วยความหนา `font-light` (300) เท่านั้น
- **งดเว้น:** ห้ามใช้ตัวเอียง (`italic`) ห้ามใช้ระยะห่างตัวอักษร (`letter-spacing`) 
- **Icon Registration Gate:** ไอคอนใช้ Heroicons แปลงผ่านระบบ CSS Mask รูปแบบ `i-*` ทุกครั้งที่มีการใช้คลาส `i-ใหม่` ต้องไปเพิ่มใน `theme.css` ก่อนเสมอ (ห้ามใช้ SVG แทรกตรงในโค้ดถ้าไม่จำเป็น)
- **Icon-Text Alignment:** ห้ามบรรทัดที่ 2 ของข้อความเลยมาอยู่ใต้ไอคอนบังคับใช้คลาส `.vs-icon-layout` จัด Flex ให้ข้อความขยับตรงบรรทัดแบบเป๊ะๆ 

---

## 3. สภาพแวดล้อมระบบล่าสุด (Current Modules Context)

### 3.1 ภารกิจและการสั่งงาน (Mission Protocol)
ปรับใหม่แยกชัดเจน 2 กลุ่ม เพื่อไม่ให้รกหน้าจอ:
1. **System Tasks (งานระบบ):** งานอัตโนมัติตามปฏิทิน (Auto-Generated), การประเมินผล KPI ใช้โทนสีเทา (Slate/Gray) ส่อความเป็นทางการ
2. **Special Missions (ภารกิจพิเศษ):** งานด่วน (Ad-hoc) ที่ถูกมอบหมายจากบุคลากรโดยตรง ใช้โทนสี Accent สะดุดตาแบบหน้าปัดเรดาร์

### 3.2 Component Handoff (Widget System)
โครงสร้างเริ่มเข้าสู่รูปแบบ **"Operating System"** มีระบบ `ComponentRegistry` ในการโหลดชิ้นส่วนหน้าต่าง (Widget) ต่างๆ ขึ้นมาบน HUD เช่น `DNA_RadarWidget`, `Fitness_DataEntryWidget` ช่วยให้โปรเจกต์กระชับขึ้นและ Reuse โค้ดได้

### 3.3 Student DNA & Data Harvesting
โมดูลหลัก (เช่น `pages/student_input.html`, `school_dna.html`, `fitness_input.html`) ผูกกับกระบวนการดึงข้อมูล `DNAHarvester.js` และ `AIAuditor.js` เข้าไปเติมแกน Intelligence DNA ทั้ง 5 (KPAED) 

---

## 4. โอกาสในการพัฒนาต่อไป (Next Action Items)
- **Student DNA Dashboard:** เชื่อมโยงข้อมูล `FitnessService` เข้าสู่แกน Physical DNA (ถ้ามี) การ์ดกราฟแสดงผลต่างๆ ให้สม่ำเสมอ
- **Transparency / Leaderboard:** หน้า `transparency_board.html` และการจัดอันดับ หรือ Log ระบบ `transparency_ledger.js`
- **ระบบ Inbox/Chat:** จัดหน้า Inbox ของ `teacher` เพื่อรองรับ Mission Protocol ภารกิจพิเศษ (Ad-hoc) 
- **Performance Monitor:** ทำ Component ให้ระบบสามารถดึงข้อมูล Memory / Storage หรือ API Calls มาเป็นส่วนของ Telemetry ที่ Admin Dashboard ได้ 
