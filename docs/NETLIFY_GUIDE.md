# 🌑 Technical Guide: Netlify Dev for WEBVI

การใช้ `netlify dev` เป็นวิธีที่แนะนำที่สุดในการพัฒนาโปรเจคนี้ เพราะจะช่วยแก้ปัญหา **CORS Conflict** ระหว่าง DLTV และ YouTube ได้อย่างถาวร โดยไม่ต้องเปิด Extension ใดๆ ใน Browser

---

## 🛠️ ขั้นตอนการติดตั้ง (ครั้งเดียว)

1. **เปิด Terminal หรือ PowerShell**
2. **ติดตั้ง Netlify CLI ทั่วทั้งเครื่อง:**
   ```bash
   npm install -g netlify-cli
   ```

---

## 🚀 วิธีการรันโปรเจค

แทนที่จะเปิดไฟล์ตรงๆ หรือรัน server อื่น ให้ใช้คำสั่งนี้ในโฟลเดอร์โปรเจค:

```bash
cd C:\Users\Zerocool\Documents\WEBVI
netlify dev
```

### สิ่งที่จะเกิดขึ้น:
*   Netlify จะรันเครื่องจำลองระบบขึ้นมาที่: **`http://localhost:8888`**
*   ระบบ **Proxy** ที่สร้างไว้ใน `netlify/functions/dltv-proxy.js` จะทำงานโดยอัตโนมัติ
*   DLTV Video จะเล่นได้โดยไม่ต้อง bypass CORS
*   YouTube และส่วนอื่นๆ จะทำงานได้อย่างสมบูรณ์ 100%

---

## 💎 ทำไมต้องใช้ Netlify Dev?

| Feature | CORS Extension | Netlify Dev (Proxy) |
| :--- | :---: | :---: |
| เล่นวีดีโอ DLTV ได้ | ✅ | ✅ |
| YouTube ใช้งานได้ | ❌ (พังบ่อย) | ✅ (ปกติ) |
| ความปลอดภัย | ⚠️ ต่ำ | 🛡️ สูง |
| จำลองสถานะตอน Deploy | ❌ | ✅ |

---

> [!TIP]
> เมื่อคุณรัน `netlify dev` แล้ว คุณสามารถปิด CORS Extension ใน Browser ได้เลยครับ ระบบจะจัดการทุกอย่างผ่าน "หลังบ้าน" ให้เองอย่างปลอดภัย
